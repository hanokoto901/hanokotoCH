name: Generate HTML

on:
  schedule:
    - cron: "0 16 * * *"  # 毎日1時（JST）※UTC 16:00 = JST 01:00
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: generate-html
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      TZ: "Asia/Tokyo"
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          set -euo pipefail
          pip install --upgrade pip
          pip install requests
          sudo apt-get update -y
          sudo apt-get install -y jq

      - name: Verify data files
        run: |
          set -euo pipefail
          echo "データファイル確認中..."
          ls -lh data/ || echo "dataディレクトリが見つかりません"
          test -f data/history.db || echo "警告: history.dbが存在しません"
          test -f data/thanks.csv || echo "警告: thanks.csvが存在しません"

      - name: Generate HTML
        run: |
          set -euxo pipefail
          python generate.py
          echo "HTML生成完了"
          ls -lh index.html

      - name: Commit and push changes
        id: commit
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add index.html
          if git diff --cached --quiet; then
            echo "変更なし"
            echo "changed=false" >> $GITHUB_OUTPUT
          else
            git commit -m "Auto-update HTML $(date '+%Y-%m-%d %H:%M')"
            git push https://x-access-token:${GITHUB_TOKEN}@github.com/${{ github.repository }}.git HEAD:main
            echo "changed=true" >> $GITHUB_OUTPUT
          fi

      - name: Set common environment variables
        if: always()
        run: |
          echo "TIMESTAMP=$(date '+%Y-%m-%d %H:%M')" >> $GITHUB_ENV
          echo "OWNER=${GITHUB_REPOSITORY%%/*}" >> $GITHUB_ENV
          echo "REPO_NAME=${GITHUB_REPOSITORY#*/}" >> $GITHUB_ENV
          echo "SITE_URL=https://${GITHUB_REPOSITORY%%/*}.github.io/${GITHUB_REPOSITORY#*/}" >> $GITHUB_ENV
          echo "RUN_URL=https://github.com/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}" >> $GITHUB_ENV
          echo "SHA_SHORT=$(echo ${GITHUB_SHA} | cut -c1-8)" >> $GITHUB_ENV

      - name: Send Discord success notification
        if: steps.commit.outputs.changed == 'true'
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
        run: |
          set -euo pipefail
          MESSAGE=$(jq -n \
            --arg title "サイトが正常に更新されました！" \
            --arg site "$SITE_URL" \
            --arg time "$TIMESTAMP" \
            --arg color "5814783" \
            '{
              "username": "HTML更新通知",
              "embeds": [{
                "title": $title,
                "color": ($color | tonumber),
                "fields": [
                  {"name": "🔗 サイトのリンク", "value": $site, "inline": false},
                  {"name": "🕒 更新日時", "value": $time, "inline": true}
                ],
                "footer": {"text": "GitHub Actions Bot"}
              }]
            }')
          curl -H "Content-Type: application/json" -X POST -d "$MESSAGE" $DISCORD_WEBHOOK

      - name: Send Discord failure notification
        if: failure()
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
        run: |
          set -euo pipefail
          MESSAGE=$(jq -n \
            --arg title "⚠️ サイトの更新に失敗しました" \
            --arg site "$SITE_URL" \
            --arg time "$TIMESTAMP" \
            --arg run "$RUN_URL" \
            --arg sha "$SHA_SHORT" \
            --arg color "16711680" \
            '{
              "username": "HTML更新通知",
              "embeds": [{
                "title": $title,
                "color": ($color | tonumber),
                "description": "詳細はワークフロー実行ログをご確認ください。",
                "fields": [
                  {"name": "🔗 サイトのリンク", "value": $site, "inline": false},
                  {"name": "🧪 ワークフロー実行", "value": ("[ログを確認](" + $run + ")"), "inline": false},
                  {"name": "🧬 コミット", "value": $sha, "inline": true},
                  {"name": "🕒 失敗検知時刻", "value": $time, "inline": true}
                ],
                "footer": {"text": "GitHub Actions Bot"}
              }]
            }')
          curl -H "Content-Type: application/json" -X POST -d "$MESSAGE" $DISCORD_WEBHOOK
